# 프로젝트 개요

이 프로젝트는 나이키 앱을 Swift로 클론코딩하는 프로젝트이다.

## 코딩 스타일 가이드

### 재사용성
- 코드를 짤 때는 재사용성을 높이는 방향으로 코드를 작성한다
- 기존 코드 중 재사용할 것이 있다면 역시 최대한 재사용한다

### 상태 관리
- `ObservableObject`와 `@Published` 대신 `@Observable`을 사용하는 것으로 코딩 스타일을 통일한다
